function getDestination(){
  // generates a random destination for your day trip
  let destinations = ['dest1', 'dest2', 'dest3', 'dest4'];
  let selection = destinations[Math.floor(Math.random() * (destinations.length))];
  return selection;
};

function getTransportaion(){
  // generates a random mode of transportation to arrive at your day trip destination
  let transport = ['mode1', 'mode2', 'mode3', 'mode4'];
  let selection = transport[Math.floor(Math.random() * (destinations.length))];
  return selection;
};

function getFood(){
  // generates a random food to eat during your day trip
  let  = ['food1', 'food2', 'food3', 'food4'];
  let selection = food[Math.floor(Math.random() * (destinations.length))];
  return selection;
};

function getFun(){
  // generates a random activity for fun on your day trip
  let fun = ['fun1', 'fun2', 'fun3', 'fun4'];
  let selection = fun[Math.floor(Math.random() * (destinations.length))];
  return selection;
};

function generateTrip(){
  //
};

function validateTrip(){
  //
};